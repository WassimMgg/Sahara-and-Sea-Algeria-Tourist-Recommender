# 📸 Screenshots

These images are embedded in the main [`README.md`](../../README.md).
To refresh one, just replace the file with a new capture using the **same name**.

## Current screenshots

| File | Page / URL | Shows |
|---|---|---|
| `home.png` | `/` | Hero banner — "Find the places made for you" |
| `recommendations.png` | `/` (logged in) | The "Picked for you" personalized strip |
| `search.png` | `/search/` | Search + filter the catalogue |
| `model.png` | `/model/` | Public algorithm comparison cards |
| `login.png` | `/login/` | "Welcome back" login form |
| `signup.png` | `/signup/` | "Create your account" form |
| `admin-dashboard.png` | `/admin/` | KPI cards + Chart.js charts |
| `admin-recommender.png` | `/admin/` → Recommender | Switch the live algorithm |
| `admin-comparison.png` | `/admin/` → Recommender | Cross-validated metrics table |
| `admin-attractions.png` | `/admin/` → Attractions | CRUD list with filters + CSV export |
| `admin-ratings.png` | `/admin/` → Ratings | CRUD list with inline star editing |
| `admin-visitors.png` | `/admin/` → Visitors | Historical raters CRUD |

> The `_unused/` folder holds extra captures that aren't shown in the README
> (an unrelated screenshot + a couple of older UI versions). Safe to delete.

## How to take a screenshot

- **Windows:** `Win + Shift + S` (Snipping Tool), then save as PNG here.
- **macOS:** `Cmd + Shift + 4`, then move the `.png` into this folder.
- **Full-page (best):** in Chrome/Edge, open DevTools (`F12`) →
  `Ctrl/Cmd + Shift + P` → type *"Capture full size screenshot"*.
